## 现有方案问题：不同路线均难以解决“无记忆、重复推理”的根本矛盾

### 1. 规则式场景感知方案

规则式场景感知通常基于预设规则、触发条件或场景模板进行判断，例如“到达某地点 + 当前时间段 + 打开某类 App”即触发某类意图判断。这类方案实现简单、响应快，但存在明显局限：

1. **规则依赖人工预设，覆盖范围有限**
   规则只能识别已经被设计好的场景，对新场景、复杂场景和用户个性化行为适应能力较弱。用户行为一旦超出预设模板，系统就难以准确判断。

2. **基于瞬时状态判断，缺乏长期行为上下文**
   规则通常关注当前时间、地点、App 状态等显式信号，难以理解用户过去一段时间的行为轨迹和长期习惯。例如同样是“晚上到达商场”，不同用户可能对应购物、吃饭、接人、运动等不同意图。

3. **规则难以自我进化，维护成本高**
   用户习惯会持续变化，规则需要人工更新和调参。随着场景数量增加，规则之间容易冲突，系统维护复杂度不断上升，难以形成“越用越懂用户”的能力。

---

### 2. 大模型实时分析方案

大模型实时分析方案通常将当前用户上下文、多模态数据或其摘要输入大模型，由模型直接推断用户意图。这类方案理解能力强、泛化能力好，但在端侧常驻意图感知场景下存在以下问题：

1. **每次判断都需要重新推理，计算成本高**
   大模型更擅长对复杂上下文进行即时理解，但如果每一次意图判断都重新组织上下文并执行推理，就会产生大量重复计算。对于高频、稳定、可复用的个人行为场景，这种方式并不经济。

2. **功耗、延迟和云侧调用成本难以支撑常驻运行**
   用户意图具有明显时间窗口，系统需要低延迟响应；但大模型推理通常计算开销较大，频繁调用会带来端侧功耗、响应延迟和云侧服务成本压力，难以支撑全天候、细粒度的实时感知。

3. **用户历史行为难以沉淀为可复用经验**
   现有大模型方案通常把历史信息作为一次性上下文输入模型，推理完成后结果并不一定转化为可长期复用的个人行为规律。也就是说，系统可能每次都“看见”用户数据，但没有真正“记住”用户习惯。

4. **隐私与数据上传压力较大**
   用户端侧多模态数据往往包含位置、App 使用、通信、日程、图片等敏感信息。如果大量上下文频繁上传云端进行分析，会带来隐私保护、数据最小化和用户信任问题。

---

### 3. 端云协同推理方案

端云协同方案通常根据任务复杂度、模型能力和设备资源，在端侧小模型与云侧大模型之间进行分工：简单任务本地处理，复杂任务上云分析。这类方案比纯云侧方案更合理，但仍存在局限：

1. **主要解决“在哪里推理”，没有解决“是否需要推理”**
   端云协同优化的是推理位置选择，即本次任务由端侧模型还是云侧模型完成；但对于大量高频、稳定、重复出现的用户意图，系统仍然可能每次都触发模型推理，缺少“记忆命中即完成”的机制。

2. **调度依据通常偏任务复杂度，缺少个人行为规律支撑**
   现有调度更多关注模型负载、网络状态、任务难度、隐私等级等因素，较少将用户长期行为规律作为调度依据。因此系统难以判断某个场景是否已经被历史经验充分覆盖。

3. **端侧与云侧结果缺乏持续回流沉淀**
   云侧大模型虽然可以处理复杂场景，但如果推理结果没有回写到端侧并转化为新的行为规律，那么下一次遇到类似场景仍然需要再次上云，无法实现云调用率随使用逐步下降。

4. **个性化能力仍依赖模型即时理解，而非长期经验积累**
   端云协同提升了模型能力和系统资源利用率，但并不天然具备“个人行为记忆”。系统可能知道如何选择模型，却不一定知道如何持续积累、修正和遗忘用户个人规律。

---

### 小结

综上，规则式方案缺乏泛化能力，大模型实时分析方案计算和隐私成本较高，端云协同方案主要优化推理位置但没有减少不必要推理。三类方案的共同问题在于：用户历史行为没有被沉淀为可写入、可读取、可进化的端侧个人行为记忆，导致每一次意图判断仍然偏向从零开始。



规则式场景感知

规则依赖人工预设与维护：场景条件（时间、地理围栏、日程）由工程师逐条定义和打磨，覆盖范围受限于预设集合，长尾场景无法触达，新场景的支持依赖版本迭代；
千人一面，缺乏个体适配：规则面向群体共性设计，无法刻画个体差异化习惯——同样"到达商场"，不同用户的意图截然不同，规则无从区分；
静态不生长：规则不随用户行为变化而更新，用户习惯改变（搬家、换工作）后规则失效，且系统无法自知。

大模型实时分析

每次判断都是从零开始的完整推理：前一次的计算结果对下一次毫无贡献，相同情境被反复推理，计算不沉淀为经验，常驻场景下功耗与成本随调用次数线性累积；
端侧与云侧各有硬伤：云侧推理受制于网络依赖、响应延时与调度成本，且原始行为数据上云带来隐私风险；端侧受限于算力与内存，小参数模型能力不足，意图判断准确性受限；
输入缺乏行为上下文与个体积累：模型仅依据当前时刻的孤立状态做判断，既不知道用户"经历了什么"，也不了解"这个用户"的历史习惯，真实场景下可靠性不足。

端云协同

任务级静态分工：按任务类型预先划分端侧与云侧职责（简单给端、复杂给云），分工不感知具体情境的确定性——对该用户已重复千次的熟悉情境，仍可能按类型被送上云；
分工不随使用进化：端云边界固定，云侧的每次分析结果未能沉淀回端侧，云调用率不会随使用时长下降，成本长期恒定；
个性化代价高：现有个性化路线多依赖端侧模型微调，将个性化写入模型参数——训练功耗高、每用户一份模型维护困难，且参数中的个性化不可解释、不可审计、难以按用户要求删除。
