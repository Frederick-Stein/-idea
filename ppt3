## 本 idea 解决方案：自进化端侧个人行为记忆体

本方案围绕“个人行为记忆体”构建端侧用户意图识别闭环，将用户历史行为从一次性数据转化为可写入、可固化、可读取、可进化的个人经验。系统由情景写入、规律固化、置信读取和后验进化四个阶段组成。

### ① 情景写入：把“经历”记下来

情景写入阶段常驻运行，采用事件驱动与周期兜底结合的混合采样机制。当场景切换、位置变化、连接状态突变或达到周期采样点时，系统对端侧多源多模态数据流进行轻量级流式处理。

在该阶段，位置、传感器、应用行为、连接环境等原始信号不会被孤立存储，而是被组织为具备时间上下文的情节级特征，例如今日轨迹序列、地点停留时长、场景转移关系、近期应用行为序列等。随后，系统在采样时刻将当前情节特征固定为一份特征快照，并写入个人历史行为库。

该阶段只写入历史行为库，不更新行为规律库、意图预取表和置信度校准表。由于该阶段仅执行轻量统计与事件采样，不触发模型推理，因此可支持端侧常驻低功耗运行。

一句话定位：情景写入产出原始“情景记忆”，记录用户经历了什么，但尚未形成稳定知识。

---

### ② 规律固化：把“经历”炼成“知识”

规律固化阶段发生在充电、息屏等低负载状态下的低成本计算窗口。系统读取历史行为库中的特征快照，并将离散经历提炼为可复用的个人行为规律。

该阶段包含三步：

第一，后验弱标注。系统为历史行为库中的每份快照 X，在其后续观察窗口内提取真实行为线索，例如打开了什么应用、到达了什么地点、完成了什么操作，并据此生成候选意图标签 Y，形成 X-Y 特征意图对。

第二，规律提炼。端侧模型对反复出现的 X-Y 模式进行归纳和聚合，生成“情境 → 意图”的行为规律条目，并写入行为规律库。每条规律包含检索向量、自然语言描述、支持度、初始命中率、最近验证时间等信息。

第三，意图预取。系统基于更新后的行为规律库，沿未来时间轴滚动预测未来一天的高概率意图，生成意图预取表。例如：“今天 18:15–18:50，预期情节为离开公司进入地铁，预测意图为通勤回家，预置置信度为 0.87”。

该阶段读取历史行为库，写入行为规律库和意图预取表。

一句话定位：规律固化对应人脑睡眠中的记忆整理过程，将情景记忆转化为语义化的个人行为知识。

---

### ③ 置信读取：用“知识”回答“此刻”

置信读取阶段发生在任何需要进行用户意图判断的时刻。系统按照计算成本从低到高逐级尝试，优先使用记忆完成判断，仅在记忆缺失或置信度不足时调用模型。

首先，系统查询意图预取表。如果当前时刻落入某个预取项的时间窗，且实际情节与预期情节相符，则直接采用预置意图，避免检索和模型推理。这是系统的最快路径。

如果预取未命中或实际情节偏离预期，系统进入常规读取路径：先生成当前特征快照，再从行为规律库中召回 top K 相似规律，结合匹配度、规律可靠度、时效性和场景一致性进行打分。随后，系统查询置信度校准表，将原始匹配分数映射为经过历史验证的校准置信度，并根据高低双阈值进行分级路由。

当校准置信度高于高阈值时，系统直接采用记忆结果；当校准置信度处于中间区间时，系统唤醒端侧模型，并将召回规律的自然语言描述作为上下文进行轻量分析；当校准置信度低于低阈值时，系统将当前快照摘要和 top K 历史规律拼入云侧模型上下文，由云侧模型进行复杂场景兜底分析。

同时，系统设置安全守门规则：如果全库相似度低于分布外底线，说明当前场景不属于已有记忆覆盖范围，强制升档到云侧分析；未完成验证的新规律不能直接输出，只能作为辅助上下文参与端侧或云侧判断。

该阶段读取意图预取表、行为规律库和置信度校准表，不直接写入三张表。本次读取产生的分数、预测结果和时间戳会被暂存，留待后验进化阶段验证。

一句话定位：高置信记忆命中时，意图判断即可完成；模型只处理记忆缺失或不确定的“缺页”场景。

---

### ④ 后验进化：让“知识”越用越准

后验进化阶段发生在每次预测后的观察窗口，以及充电、息屏等低负载状态下的批处理窗口。系统通过真实后续行为验证前一次意图判断，并据此更新个人行为记忆体。

该阶段包含三步：

第一，后验验证。系统根据观察窗口内用户的真实行为判断上次预测是否命中，并更新命中规律的元数据，例如历史命中率、最近验证时间、累计使用次数和时效权重。同时，系统更新置信度校准表中对应分数桶的真实命中统计，并根据目标命中率自动调整高低双阈值。

第二，教师蒸馏。对于低置信度上云的复杂案例，云侧模型的分析结果不会只作为一次性输出，而会在闲时被提炼为新的候选规律，写回端侧行为规律库。这样，云侧模型的推理结果可以沉淀为端侧可复用知识，减少未来类似场景的云调用。

第三，遗忘淘汰。对于长期未被验证、近期命中率下降或连续预测失败的规律，系统按照时效衰减机制降低其权重；当规律长期失效时，将其从高置信候选中移除，直至淘汰出库。

该阶段拥有行为规律库、意图预取表和置信度校准表的修订权限，是记忆体持续自我更新的关键阶段。

一句话定位：后验进化实现记忆的再巩固与遗忘，使系统越用越准、越用越省。
